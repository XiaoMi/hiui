# Pagination 分页

用来分隔一个长列表，每次只加载一个页面，从而可释放后端服务器加载压力

## 何时使用

当数据量级较大，请求数据时延过长

当展示数据的页面空间有限，无法一次性展示时

用户更关心前几条或前几十条数据情况时

## 使用示例

<!-- Inject Stories -->

## Props

<!-- Inject Props -->

## Events

| 名称             | 说明                       | 类型                                                      | 参数                                                                        | 返回值 |
| ---------------- | -------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------- | ------ |
| onJump           | 快速跳转时触发             | (current: number) => void                                 | current: 快速跳转页数                                                       | -      |
| onChange         | 页码改变时的回调           | (current: number, prev: number, pageSize: number) => void | current: 改变后的新页数 <br/> prev: 改变前的页数 <br/> pageSize: 每页记录数 | -      |
| onPageSizeChange | 每页显示条数改变的回调函数 | (pageSize: number, current: number) => void               | current: 当前页数 <br/> 每页记录数                                          | -      |

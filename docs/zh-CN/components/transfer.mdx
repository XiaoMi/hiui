# Transfer 穿梭框

在一定展示空间里对选项进行单个或批量移动从而完成挑选的数据容器

## 何时使用

- 一组数据进行两种状态的分类时
- 有更多的空间进行选择时
- 需快速批量的完成选项归类且可调整顺序时

## 基础用法

import DemoBase from '../../demo/transfer/section-base.jsx'

<DemoBase />

## 批量

import DemoBatch from '../../demo/transfer/section-batch.jsx'

<DemoBatch />

## 全选

import DemoCheck from '../../demo/transfer/section-check.jsx'

<DemoCheck />

## 搜索

import DemoSearch from '../../demo/transfer/section-search.jsx'

<DemoSearch />

## 目标数量上限

import DemoCount from '../../demo/transfer/section-count.jsx'

<DemoCount />

## 目标区域拖拽

import DemoArea from '../../demo/transfer/section-area.jsx'

<DemoArea />

## Props

| 参数         | 说明                                                                       | 类型                      | 可选值                  | 默认值    |
| ------------ | -------------------------------------------------------------------------- | ------------------------- | ----------------------- | --------- |
| type         | 类型                                                                       | string                    | 'default' \| 'multiple' | 'default' |
| showCheckAll | 是否展示全选按钮                                                           | boolean                   | true \| false           | false     |
| title        | 标题(数组长度为 1 或 2 位，1 位时左右标题将相同，2 位时将使用对应索引标题) | string [] \| ReactNode [] | -                       | -         |
| searchable   | 是否可筛选                                                                 | boolean                   | true \| false           | false     |
| emptyContent | 数据为空时的显示内容                                                       | string[] \| ReactNode[]   | -                       | -         |
| disabled     | 是否禁用                                                                   | boolean                   | true \| false           | false     |
| data         | 数据集合                                                                   | string                    | DateItem[]              | -         |
| targetLimit  | 最大可穿梭上线                                                             | number                    | -                       | -         |
| targetIds    | 目标框内的元素 id 集合                                                     | number[] \| string[]      |                         | -         |
| onChange     | 选中元素被移动到目标框内后的回调                                           | (targetKeys) => void      | -                       | -         |

## Type

**_DataItem_**

| 参数     | 说明     | 类型                | 可选值        | 默认值 |
| -------- | -------- | ------------------- | ------------- | ------ |
| id       | 唯一 id  | string              | number        | -      |
| content  | 内容     | string \| ReactNode | -             | -      |
| disabled | 是否禁用 | boolean             | true \| false | false  |

# SelectTreeDialog 多选树状弹窗

**多选树状弹窗**是基于`Tree`组件的一层封装，其大致用法与`Tree`组件类似

## 何时使用

当数据为**树状结构**，并且需要勾选多个其子项

场景举例：手机型号勾选（树形结构，叶节点为具体机型）

适用数据大小：**小中**数量级（考虑到大数量级的性能问题，本组件不适用）

## 基础风格

import SimpleStyleDemo from '../../demo/select-tree-dialog/simpleStyleDemo.jsx'

<SimpleStyleDemo />

## 线框风格

import WithBorderStyleDemo from '../../demo/select-tree-dialog/WithBorderStyleDemo.jsx'

<WithBorderStyleDemo />

## 注意点

- 组件处于功能设计考虑，**不支持懒加载**节点数据，故而使用的时候，需要一次性传递所有数据
- 组件最终只会告知外部哪些**叶节点被勾选**（不会传输被勾选的父节点）
- 勾选父节点相当于**全选**其子节点
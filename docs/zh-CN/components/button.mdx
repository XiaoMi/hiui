# Button 按钮

常用的操作按钮。

## 标准按钮

- 突出按钮：用于较重要的功能，需要强烈引导用户点击的功能操作。
- 普通按钮：用于不重要的功能，不需要太引导用户点击的功能操作。
- 默认按钮：用于不重要的功能，不需要太引导用户点击的功能操作。
- 确定/取消按钮：适用于表单、弹窗场景。

import DemoStandard from '../../demo/button/section-standard.jsx'

<DemoStandard />

## 辅助按钮

import DemoAux from '../../demo/button/section-aux.jsx'

<DemoAux />

## 链接按钮

import DemoLink from '../../demo/button/section-link.jsx'

<DemoLink />

## 按钮尺寸

import DemoSize from '../../demo/button/section-size.jsx'

<DemoSize />

## 按钮组

import DemoGroup from '../../demo/button/section-group.jsx'

<DemoGroup />

## 加载中状态

import DemoLoading from '../../demo/button/section-loading.jsx'

<DemoLoading />

## Props

| 参数       | 说明                                          | 类型                    | 可选值                                                    | 默认值    |
| ---------- | --------------------------------------------- | ----------------------- | --------------------------------------------------------- | --------- |
| type       | 设置按钮类型                                  | string                  | 'primary' \| 'line' \| 'success' \| 'danger' \| 'default' | 'default' |
| appearance | 设置按钮外观                                  | string                  | 'link' \| 'button'                                        | 'button'  |
| size       | 设置按钮尺寸                                  | string                  | 'large' \| 'default' \| 'small'                           | 'default' |
| icon       | 设置按钮图标                                  | string                  | 详见 [`<Icon/>`](/hiui/zh-CN/docs/icon) 组件 ︎            | -         |
| href       | 设置按钮链接，设置后将用 a 标签渲染按钮       | string                  | ︎                                                         | -         |
| target     | 同 a 标签的 target 属性，仅在设置 href 后有效 | string                  | ︎'\_self' \| '\_blank' \| '\_parent' \| '\_top'           | -         |
| loading    | 是否显示 loading                              | boolean                 | ︎-                                                        | false     |
| onClick    | 点击按钮时的回调                              | (e: MouseEvent) => void | -                                                         | -         |

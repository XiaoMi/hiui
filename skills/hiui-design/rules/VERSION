version: 1.0.0
last-updated: 2026-07-06
source-snapshot: machine planning entry, fixture-backed public JSON contracts, runtime/preview/start contract fixtures, and global-sync-safe governance docs
machine-facts-file: rules/common.page-types.json
machine-public-cli: shipped plan-page-task, typical-page-runtime-smoke, typical-page-preview-ready, typical-page-start-page
notes:
  - promoted scripts/tests/fixtures/public-cli/*.json to explicit open-source distribution assets and added a fail-closed maintainer check so shipped machine-public contract fixtures cannot drift out of the public package
  - enforced a maintainer-source version gate so guarded changes under rules/, scripts/, SKILL.md, and distribution-manifest.json must bump rules/VERSION before sync or release
  - restored fail-closed runtime delivery on vendored typical-page-shells tgz for open-source sync, including legacy-host-hard-gates and project-facts runtime dependencies
  - split maintainer runtime out of skill-root outputs into ~/.codex/maintainers/hiui-design/runtime, with HIUI_DESIGN_MAINTAINER_HOME as the explicit override point
  - reorganized scripts/lib into shared, public, and maintainer layers while keeping root-level shim exports for compatibility
  - added maintainer-side qualification checks so activate-current-project-global-sync and launch-agent activation reject incomplete or version-stale local skill mirrors before they can become the machine-level source
  - expanded release and global-sync pipelines to refresh hiui-v5 manifest facts, quick reference, component map, and strict knowledge coverage before archive rebuilds
  - introduced hiui-v5 component knowledge manifests and generated mapping docs so kickoff, routing, and validation can point to a single maintained facts layer instead of hand-maintained scattered references
  - upgraded select-archetype to emit task-router contract facts, escalation triggers, and delivery/usage state models, and added drift-guard regressions to keep that public contract stable
  - added fixture-backed public JSON contracts for runtime-smoke, preview-ready, and start-page using explicit contract-fixture modes so machine consumers can drift-test stable output without mutating target projects
  - added plan-page-task as the default machine planning entry for page work, with fixture-backed JSON coverage for task level, mode, page type, required docs, commands, contract fields, and blocking reasons
  - extended maintainer regression coverage to include script-layer boundary checks, router contract drift, generated-doc drift, and usage-runtime state recovery
  - clarified host-integration apply/setup behavior so browser reuse and preview opening are explicit, and aligned onboarding docs with the new maintainer runtime/log paths
  - kept rules/page-type map and generated figma docs on the rules/VERSION line so release artifacts and derived docs remain version-aligned with the active rules snapshot

# Page Type Map

这份文件只保留兼容入口，不再承载独立页型规则。

- AI 默认判型请直接读取 [`../../rules/page-type-map.md`](../../rules/page-type-map.md)
- 默认生成边界请读取 [`../../rules/generation-rules.md`](../../rules/generation-rules.md)
- 命中页型后，再继续读 [`figma-page-rules.md`](./figma-page-rules.md) 和对应 `figma-pages/*.md`

不要把本文件当成第二份权威页型总表；它只用于兼容旧引用。

# HiUI Design Explainers

本目录承接复杂场景下供 Agent / 维护者补读的解释层文档。

约束：

- 本目录不是 source of truth。
- 字段语义、gate、contract、页面生命周期与资产协议，仍以 `rules/**` 与对应 `docs/generation/**` owner 文档为准。
- 本目录只解释“为什么这样生成”“复杂模式下应如何补读”，不得重新定义一套平行规则。
- `reference/` 目录只承载参考资产与 reference-only 基线；不要把 explainers 放回顶层 `references/` 或混进 `reference/`。

当前 explainers：

- `core-object-model.md`
- `standard-generation-protocol.md`
- `generation-gates.md`
- `delivery-gates.md`

# Adapter Contract

An adapter contract is the auditable record that binds:

- one canonical example page
- one canonical page-type archetype
- one target host archetype
- one region mapping
- one ownership mapping

Without this contract, a generated page is not treated as example-aligned.

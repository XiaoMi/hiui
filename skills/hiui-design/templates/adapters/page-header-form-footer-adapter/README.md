# Page Header + Form + Footer Adapter

Use this adapter when a host already has:

- a page header slot with back handling
- a white-body workspace container
- a scrollable form body
- a sticky footer slot

It is the default adapter family for `full-page-edit`.

interface:
  display_name: "体验走查"
  short_description: "对代码库、URL 或截图做体验走查；输出完整报告并生成本地 docx"
  default_prompt: "以 SKILL.md 为唯一主规范，按 A→B→C 三阶段执行。优先使用 precheck_walkthrough.py 与 generate_docx.py；docx 未生成成功前，不得将任务视为完整完成。"
policy:
  allow_implicit_invocation: true

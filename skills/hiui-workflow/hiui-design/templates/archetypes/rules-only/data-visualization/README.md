# Rules-only Data Visualization Template

Strict-example pages in `rules-only` mode must start from `page.template.tsx` in this directory. `typical-page:start-page` is not allowed to synthesize a free-form dashboard scaffold for this archetype.
